"""HermesContext — RAG MCP Server, 100% self-hosted.

Core Layer (business logic, no interface dependency):
    config, database, embeddings, engine, utils

Interface Layer (consumes Core):
    server (MCP), cli (CLI)
"""
